export declare class PaymentCalculatorService {
    calculatePayment(distance: number, pax: number, cargo: number, aviationType: number, firstClass?: number): number;
    private getAviationMultiplier;
}
